#include "main.h"

/**
 * *_strcpy - copies the string pointed to by src, including \0.
 *@src: pointer.
 *@dest: pointer.
 *Return: the pointer to dest.
 */

char *_strcpy(char *dest, char *src)
{
	int x,lngh;

	for (lngh = 0; src[lngh] != '\0'; lngh++)
	{
	}

	for (x= 0; x<= lngh ; x+)
	{
		dest[x = src[x]];
	}
	return (dest);
}
