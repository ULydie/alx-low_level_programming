#include "main.h"

/**
 * add - adds two integers and returns the result
 * @w: int to be added to q
 * @q: int to be added to w
 * Return: sum of w and q
 */
int add(int w, int q)
{
int x;
x = w + q;
return (x);
}
